/*==========================================================
Đơn thức là biểu thức đạị số chỉ gồm một số, 1 biến hoặc 1 tích giữa các số và các biến hay là 1 hạng tử. Để đơn giản ta chỉ quan tâm đến đơn thức chỉ bao gồm 1 số (hệ số) và luỹ thừa của x, ví dụ: (xem trong đề)

Như thế, để biểu diễn một đơn thức ta chỉ cần lưu hệ số và luỹ thừa (hay bậc) của x.

Hãy viết khai báo struct DonThuc gồm các trường:

he_so: số thực
bac: số nguyên
==========================================================*/

struct DonThuc{
	float he_so;
	int bac;
};